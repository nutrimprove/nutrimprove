import { withStyles } from '@material-ui/core';
import AddRecommendations from './AddRecommendations';
import styles from './AddRecommendations.styles';

export default withStyles(styles)(AddRecommendations);
