import { withStyles } from '@material-ui/core';
import MenuButton from 'components/Header/MainNav/MenuButton/MenuButton';
import styles from 'components/Header/MainNav/MenuButton/MenuButton.styles';

export default withStyles(styles)(MenuButton);
